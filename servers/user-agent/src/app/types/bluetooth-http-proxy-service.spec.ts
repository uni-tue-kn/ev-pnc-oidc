import { BluetoothHttpProxyService } from './bluetooth-http-proxy-service';

describe('BluetoothHttpProxyService', () => {
  it('should create an instance', () => {
    // expect(new BluetoothHttpProxyService()).toBeTruthy();
  });
});
