import { EvAuthorizationDetail } from './ev-authorization-detail.class';

describe('EvAuthorizationDetails', () => {
  it('should create an instance', () => {
    expect(new EvAuthorizationDetail()).toBeTruthy();
  });
});
